Inside your repository, run: 
```shell
git config core.hooksPath <location-of-the-hooks-folder> 
```
To add this repository to the hooks path. 

or, run 
```shell
git config --global core.hooksPath <location-of-the-hooks-folder> 
```
To add these hooks to all the git projects.